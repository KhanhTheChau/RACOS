version: "3.1"

rules:
  - rule: Activate DHCT Action
    steps:
      - intent: DHCT
      - action: action_DHCT

  # - rule: Handle out-of-scope messages
  #   steps:
  #     - intent: out_of_scope
  #     - action: action_default_fallback
